Deck list: one price, honest completion text, two bare buttons

All three from Zach's screenshot of the deployed build.

ONE DOLLAR FIGURE. "I didn't want 2 dollar amounts just the total cost that's
it." The row showed value AND shortfall; now it shows what the deck is worth.

READY TO PLAY MEANT "NOTHING MISSING FROM THE LIST", WHICH IS NOT THE SAME
THING. "both avatar aang and Hashaton say ready to play when they are 1%
complete that doesn't make sense."

Both decks hold ONE card and own it, so missing = listed - owned = 0 and the
row concluded the deck was playable -- while the ring beside it correctly read
1%. Two figures from the same data contradicting each other in the same row.

A deck is ready when its OWNED cards reach its target size, and not before.
Those rows now read "1 of 100 cards", which agrees with the ring.

THE SELECTION BAR IS NOW JUST THE TWO BUTTONS. "get rid of all the text that
says how many cards just delete and export."

It carried a selected-deck count, a large copy total, and a line explaining the
one-copy-per-deck rule. I added that explanation on the principle that a number
is only trustworthy if the rule behind it is visible -- true, but he was not
asking for the number at all, and the right home for that sentence is the
export sheet where the buylist is actually produced.

Removed the now-dead totalCopies and missing computations rather than leaving
them to run on every render for nothing.

TESTS: DC-TC9, DC-TC10, DC-TC11, each verified by reintroducing the exact
behaviour Zach objected to -- a one-card deck claiming to be ready, a second
dollar figure, and bar text creeping back.

343 tests pass, lint and build clean.
