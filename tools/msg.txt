Restyle the scanned list to match the rebuilt screens

Zach: "scanned list should get a restyle to follow suit."

PRESENTATION ONLY. This is the screen where a wrong record costs a recount
against cardboard, so the diff was checked for logic rather than eyeballed for
colour: every non-style change is a button className swap or a `type="button"`
reflow. Verified still in place afterwards -- flagged rows first, the 33-49
weak-match band, solid-yellow borders for unresolved and dashed for weak.

WHAT CHANGED:

  * --type-grass -> --accent-green. The old name is from the Pokemon-era
    palette; index.css already aliases it, but a Magic card has no grass type
    and the rebuilt screens do not use it.
  * --text-strong -> --text-primary, hardcoded rgba panels -> --surface-N,
    literal radii -> --radius-*.
  * Rows drop .glass-panel for an explicit --surface-1 + --radius-md, which is
    how Collection and the deck screens draw panels.
  * .btn / .btn-primary / .btn-secondary become local flat shapes.
    .btn-primary is a red gradient with a hover lift and a coloured shadow
    (index.css:491) -- the most visible "different generation" tell on the
    screen.

The button shapes are LOCAL, not a redefinition of .btn in index.css. Those
classes are used by screens that have not been rebuilt or reviewed, and Zach
reviews the deployed build screen by screen; a change that silently reaches
screens neither of us is looking at is exactly the kind not to make.

Z-INDEX NOW USES THE SHARED CONSTANT. The overlay was a literal 1100 -- correct,
and hard-won: Zach reported "Scanned page is still not functional no add all or
you can see some of the other buttons are covered" when the nav drew over it.
zLayers.js exists so that value lives in one place with the reason attached,
and fixedSurfaceStacking.test.js enforces it -- so the one screen already
bitten by this was the one screen the guard could not see. Now Z_FULLSCREEN.

CAUGHT BEFORE SHIPPING: the className swap left three buttons with TWO style
props. React keeps the last silently, so those three would have rendered
unstyled while looking correct in the source. Only eslint's
jsx-no-duplicate-props saw it -- the build was happy.

343 tests pass, lint and build clean.
