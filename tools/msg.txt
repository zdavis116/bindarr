Fix the same completion bug on the dashboard; show total deck value

Zach: "The dashboard still shows the deck list percentage in correctly."

I fixed DeckList and never grepped for the other reader. Dashboard.jsx had the
identical (total_cards / target_size), so his Tony Stark deck showed 97% there
too -- same cause, same lie, one screen over.

The ownership rule was put in SQL precisely so every screen reads ONE number.
Fixing one component and not the other is the drift that design was meant to
prevent, and a per-file fix cannot enforce it. So DC-TC7 now checks EVERY
component for a completion figure derived from cards LISTED rather than cards
OWNED. Verified by restoring the exact dashboard bug and watching it fail.

The dashboard's "X of Y cards" line had the same fault and now reads owned too.

TOTAL DECK VALUE.

Zach: "I wanted the price on the deck list to show the total cost of the deck
not just the missing total cost."

I built the missing-cost figure because I was thinking about the buylist; he
was asking what the deck is worth. Both numbers already came from the endpoint,
so this is a display change. The row now leads with the value and qualifies it
with the shortfall -- "$181.78 · 94 missing · $174.06" -- because a deck's
worth and its cost to finish are two different questions and collapsing them
would lose whichever one he needed.

DC-TC8 initially PASSED while the displayed value was hardcoded to zero,
because it only asserted that the identifier `deck.deckValue` appeared
somewhere in the file. A test that a name exists is not a test that a number
reaches the screen. It now checks the rendered expression, and was verified by
hardcoding the value and watching it fail.

343 tests pass, lint and build clean.
