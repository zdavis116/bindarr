# Dependency directories
node_modules/
jspm_packages/
web_modules/

# Production builds
dist/
build/
out/

# Database files
*.db
*.db-journal
*.sqlite
backend/database/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
logs/
*.log

# Agent instructions
CLAUDE.md

# One-off dev scratch (ad-hoc patch scripts and scratch test runners).
# The real test suite lives in backend/test/ and is NOT matched by these.
patch*.py
/backend/test.js
/backend/test[0-9].js

# IDE files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# AI/Agent directories
.agents/
.claude/
.codegraph/
.gemini/
.cursor/
.aider/

# Agent-workflow scratch docs (regenerated per run; not shipped)
TEST_INFRA.md
TEST_READY.md

# Prebuilt scan-match data (embeddings, ORB indexes, per-set caches) — large, regenerable
backend/data/

# Tesseract language data (PR 8 collector-number OCR). ~5MB, downloaded on
# first use and cached. It normally lands in backend/data/ocr (already ignored
# above via backend/data/), but tesseract.js falls back to the process CWD when
# it cannot write there, so this catches the stray copy rather than letting a
# 5MB binary blob into a commit.
*.traineddata

# Capacitor: build artifacts + web copies are handled by the per-platform
# .gitignore files under frontend/android and frontend/ios. Only add here what
# they miss: never commit a release signing keystore.
*.keystore

# Build artifacts / secrets (never commit)
app-release.aab
asckey.b64

# Card art cached by backend/test/crop.test.js (re-downloaded on demand)
.bench-cache/

# Scanner rebuild: build inputs and derived artefacts.
# The bulk file (74MB) is a download; the index (16MB JSON) is derived and
# will ship as a packed ~2MB binary in Phase 3. Neither belongs in git.
hash-index/
.hermes/

# Phase 4a spike: vendored onnxruntime + model binaries (32MB). The spike page
# itself is tracked; the binaries are fetched by backend/spike/phase4a/fetch.sh
backend/spike/phase4a/*.wasm
backend/spike/phase4a/*.onnx
backend/spike/phase4a/*.mjs
yolo11n-obb.pt
*.pt
