name: Build and Publish Docker Image

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]
  # Lets a tag be rebuilt/re-tagged without moving the git tag (run this
  # workflow against a v* ref to republish its image tags).
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install backend dependencies
        run: npm ci --onnxruntime-node-install=skip
        working-directory: backend

      # Known advisories are tracked separately so dependency upgrades remain
      # reviewable. Keep the baseline visible without blocking unrelated PRs.
      - name: Audit backend runtime dependencies (baseline)
        continue-on-error: true
        run: npm audit --omit=dev --audit-level=high
        working-directory: backend

      # Hard gate: backend tests must pass before an image is built/pushed.
      - name: Backend tests
        run: npm test
        working-directory: backend

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Audit frontend runtime dependencies (baseline)
        continue-on-error: true
        run: npm audit --omit=dev --audit-level=high
        working-directory: frontend

      # Enforcing: the frontend lints clean (0 problems). Keep it that way.
      - name: Lint frontend
        run: npm run lint
        working-directory: frontend

      - name: Check frontend locales
        run: npm run check:locales
        working-directory: frontend

      - name: Build frontend
        run: npm run build
        working-directory: frontend

  build-and-push:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          # `latest` follows the newest RELEASE, not main. The README tells
          # self-hosters to run :latest, and main can contain unreleased work —
          # gating it on refs/heads/main silently handed them untagged code.
          # It also meant `latest` and the version numbers were applied by two
          # separate runs (main push, then tag push), so they landed on two
          # different digests and never appeared together in the registry.
          # Main now publishes `edge` for anyone who wants to track it.
          tags: |
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=edge,enable=${{ github.ref == 'refs/heads/main' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
